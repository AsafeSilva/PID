Windows janelaEsquerda, janelaInput, janelaOutput;

void setup(){
    size(900, 600);
    
    //for(int i = 0; i < width; i += 10)  line(i, 0, i, height);
    //for(int i = 0; i < height; i += 10)  line(0, i, width, i);
    
    janelaEsquerda = new Windows(10, 10, width/4.5, height-20);
    janelaEsquerda.createWindow();  
    janelaInput = new Windows(220, 10, 670, height/2);
    janelaInput.createWindow();    
    janelaOutput = new Windows(220, height/2+30, 670, height/2-40);
    janelaOutput.createWindow();  
}

void draw(){
  
}